# دليل تشغيل ونشر "منصة ختم" على Vercel وقواعد البيانات السحابية 🚀

أهلاً بك! عند نشر هذا المشروع على منصة **Vercel**، قد تلاحظ أن البيانات التي تضيفها (مثل الموظفين الجدد أو العملاء أو تصميم البطاقات) لا تُحفظ بشكل دائم بعد إغلاق المتصفح أو بعد بضع دقائق.

في هذا الدليل، سنشرح لك **السبب** وكيف صممنا لك **حلاً ذكياً وتلقائياً**، بالإضافة إلى خطوات بسيطة لربط قاعدة بيانات سحابية حقيقية ومجانية بالكامل!

---

## 1. لماذا لا تُحفظ البيانات بشكل دائم على Vercel افتراضياً؟
منصة **Vercel** هي منصة تشغيل سحابية بدون خادم (Serverless). هذا يعني:
* **بيئة تشغيل مؤقتة (Ephemeral):** عندما تطلب الصفحة، يقوم Vercel بإنشاء حاوية تشغيل صغيرة لتشغيل خادم Node.js الخاص بك، وعندما يتوقف النشاط، يقوم بإغلاق هذه الحاوية تماماً لحفظ موارد الخادم.
* **نظام ملفات للقراءة فقط (Read-Only File System):** لتأمين وحماية خوادمه، يمنع Vercel أي تطبيق من الكتابة أو التعديل على الملفات داخل خوادمه. لذلك، ملف `db.json` يكون للقراءة فقط ولا يمكن التعديل عليه أثناء التشغيل السحابي.

---

## 2. الحل الذكي المدمج في النظام (Automatic Client-Side Fallback)
لتفادي هذه المشكلة تماماً وتسهيل تجربة وعرض المنصة دون الحاجة لإعداد خوادم معقدة، قمنا ببناء **بنية هجينة ذكية** داخل الأكواد (`dashboard.js`, `scanner.js`, `card.html`):
* **كشف تلقائي للأخطاء:** إذا حاول النظام الاتصال بالخادم ولم ينجح، أو إذا واجه الخادم مشكلة في الكتابة على ملف `db.json` السحابي، سيتعرف الكود تلقائياً على ذلك.
* **تفعيل وضع الحفظ المحلي (LocalStorage Fallback):** سيتحول النظام بالكامل وبشكل فوري لحفظ جميع بياناتك (التاجر، الموظفين، الصلاحيات، الأختام، وتصميم البطاقة) داخل متصفحك بشكل آمن!
* **عرض وتجربة كاملة:** يمكنك تجربة تسجيل الدخول، تصميم بطاقتك وتعديل ألوانها، وإضافة موظفين، وتوليد بطاقة العميل بـ QR، ومسح الـ QR وختم البطاقة من صفحة الكاشير بالصوت وحركة الختم — **كل ذلك سيعمل 100%** وسيحفظ البيانات في متصفحك حتى لو كان السيرفر معطلاً أو للقراءة فقط!

---

## 3. كيف تجعل النظام يعمل بقاعدة بيانات سحابية حقيقية؟ (للإطلاق الفعلي للعملاء)
إذا كنت جاهزاً لإطلاق مشروعك التجاري لعملاء حقيقيين، وتريد حفظ البيانات في قاعدة بيانات سحابية دائمة وآمنة، فاتبع هذه الخطوات البسيطة لربط قاعدة بيانات **MongoDB Atlas** (مجانية بالكامل مدى الحياة):

### الخطوة الأولى: إنشاء قاعدة بيانات مجانية على MongoDB Atlas
1. توجه إلى [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) وأنشئ حساباً مجانياً.
2. أنشئ عنقوداً جديداً (Create a Free Cluster).
3. من تبويب **Database Access**: أضف مستخدم جديد واكتب له اسم مستخدم وكلمة مرور.
4. من تبويب **Network Access**: اضغط **Add IP Address** واختر السماح بالوصول من أي مكان (`0.0.0.0/0`) لكي يتمكن خادم Vercel من الاتصال بها.
5. اضغط على زر **Connect** ثم اختر **Drivers (Node.js)** وانسخ رابط الاتصال (Connection String) الذي سيبدو هكذا:
   ```text
   mongodb+srv://<username>:<password>@cluster0.xxxx.mongodb.net/khatm?retryWrites=true&w=majority
   ```

### الخطوة الثانية: تعديل بسيط على ملف `server.js` لقراءة البيانات من MongoDB
افتح ملف `server.js` وقم بتثبيت مكتبة `mongoose` بـ:
```bash
npm install mongoose
```

ثم استبدل دوال قراءة وكتابة البيانات بـ `mongoose`:
```javascript
const mongoose = require('mongoose');

// ربط الاتصال بـ MongoDB السحابية عبر متغير البيئة
const MONGO_URI = process.env.MONGODB_URI;
mongoose.connect(MONGO_URI, { useNewUrlParser: true, useUnifiedTopology: true })
  .then(() => console.log('✅ متصل بقاعدة بيانات MongoDB السحابية بنجاح!'))
  .catch(err => console.error('❌ فشل الاتصال بقاعدة البيانات السحابية:', err));

// تعريف جداول البيانات (Schemas)
const MerchantSchema = new mongoose.Schema({ /* الحقول */ });
const CustomerSchema = new mongoose.Schema({ /* الحقول */ });
// ... وبالمثل لباقي الجداول ...
```

### الخطوة الثالثة: إضافة رابط قاعدة البيانات في Vercel
1. افتح لوحة تحكم مشروعك في [Vercel Dashboard](https://vercel.com).
2. اذهب إلى **Settings > Environment Variables**.
3. أضف متغيراً جديداً:
   * **Name:** `MONGODB_URI`
   * **Value:** رابط الاتصال بـ MongoDB الذي نسخته في الخطوة الأولى (بعد استبدال كلمة المرور واسم المستخدم ببياناتك الحقيقية).
4. اضغط **Save** ثم قم بإعادة نشر المشروع (Redeploy).

بمجرد إتمام هذه الخطوات، ستعمل جميع الـ APIs الخاصة بك بسلاسة فائقة، وستُحفظ بيانات كافيهاتك وعملائك بشكل فوري ودائم في السحاب، ليكون مشروع "ختم" جاهزاً لاستقبال آلاف الكافيهات والصالونات في الخليج! 🇸🇦✨
